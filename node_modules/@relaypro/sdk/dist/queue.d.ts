interface Params {
    max?: number;
}
declare type fn = {
    (): PromiseLike<void>;
};
export default class Queue {
    private max;
    private numActive;
    private queue;
    constructor(params?: Params);
    private next;
    private execute;
    enqueue(fn: fn): void;
}
export {};
